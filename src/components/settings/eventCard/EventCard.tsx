import React from 'react';

import CSS from './EventsSettingsCards.module.scss';
import { EventCardProps } from './EventCard.d';
const {
    cssClass: s_cssClass,
} = CSS;

export default function(props: EventCardProps): JSX.Element {
    
    return (
        <div>
            
        </div>
    );
}